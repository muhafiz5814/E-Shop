<%- include("partials/header") %>

<body>
    <div class="text-center mt-5">
        <a class="btn btn-secondary" href="/seller/products/add" role="button">Add Product</a>
      </div>

      <div class="album py-5 bg-body-tertiary">
        <div class="container">
            <!-- Check if products array is empty or not -->
            <% if (products) { %>
                <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 g-3">
            
                    <% products.forEach(product => { %>
                        <div class="col">
                            <div class="card shadow-sm">
                              <h5 class="card-title ms-3"><%= product.title %></h5>
                              <div class="card-body">
                                <p class="card-text"><%= product.description %></p>
                                <div class="d-flex justify-content-between align-items-center">
                                  <small class="text-body-secondary">Price: $<%= product.price %></small>
                                  <div class="btn-group">
                                  </div>
                                </div>
                              </div>
                            </div>
                        </div>
                    <% }) %>
                    
                </div>
            <% } %>

          

        </div>
      </div>
</body>
</html>